# 👕 T-Shirt Inventory Database (MySQL)

A comprehensive SQL script for setting up and populating a simple t-shirt inventory database. This project demonstrates core database management skills (DDL, DML) and efficient handling of sample data.

## ✨ Features

* **Database Setup:** Creates the `tshirt_database` and selects it for use.
* **Schema Definition (DDL):** Defines the `tshirt_Table` with an **`AUTO_INCREMENT` Primary Key** (`shirt_Id`).
* **Data Integrity:** Enforces constraints like `NOT NULL` and utilizes efficient data types (`SMALLINT` for `last_Worn`).
* **Schema Flexibility:** Includes an `ALTER TABLE` statement to safely modify the `shirt_Size` column size.
* **Data Population:** Inserts **248 sample records** using efficient multi-row `INSERT INTO` syntax.
* **CRUD Demonstration:** Concludes with fundamental `SELECT` and `UPDATE` statements to demonstrate basic data operations.

## 🚀 How to Use

1.  Clone this repository.
2.  Open the file in a MySQL client (Workbench, CLI, etc.).
3.  Execute the entire script sequentially.

---

# 👕 Base de Datos de Inventario de Camisetas (MySQL)

Script SQL completo diseñado para crear e inicializar una base de datos simple para el inventario de camisetas. Este proyecto demuestra habilidades esenciales en la gestión de bases de datos (DDL, DML) y el manejo eficiente de datos de muestra.

## ✨ Características

* **Configuración de la Base de Datos:** Crea la base de datos `tshirt_database` y la selecciona para su uso.
* **Definición de Esquema (DDL):** Define la tabla `tshirt_Table` con una **Clave Primaria `AUTO_INCREMENT`** (`shirt_Id`).
* **Integridad de Datos:** Aplica restricciones como `NOT NULL` y utiliza tipos de datos eficientes (`SMALLINT` para `last_Worn`).
* **Flexibilidad de Esquema:** Incluye una sentencia `ALTER TABLE` para modificar de manera segura la longitud de la columna `shirt_Size`.
* **Población de Datos:** Inserta **248 registros de muestra** utilizando la sintaxis eficiente `INSERT INTO` para múltiples filas.
* **Demostración CRUD:** Concluye con sentencias básicas `SELECT` y `UPDATE` para demostrar las operaciones fundamentales sobre los datos.

## 🚀 Cómo Usar

1.  Clona este repositorio.
2.  Abre el archivo en un cliente de MySQL (Workbench, CLI, etc.).
3.  Ejecuta el script completo de forma secuencial.
